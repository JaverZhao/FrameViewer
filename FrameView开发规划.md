# Windows 轻量化序列帧查看工具开发规划

## 一、项目目标

开发一个 Windows 系统下的轻量化序列帧查看工具，用于快速预览 PNG / JPG / WebP 等图片序列帧动画效果。

工具核心目标：

1. 快速打开序列帧文件夹。
2. 自动识别连续帧。
3. 按指定 FPS 播放。
4. 支持播放、暂停、上一帧、下一帧、循环播放。
5. 支持缩放、适配窗口、查看透明背景。
6. 保持软件轻量、启动快、内存占用可控。
7. 后续可扩展为专业素材预览工具。

---

## 二、技术栈选择

### 主技术栈

* 开发语言：C#
* 运行框架：.NET 8 或 .NET 9
* 桌面框架：WPF
* UI 架构：MVVM
* MVVM 工具：CommunityToolkit.Mvvm
* 图片解码：

  * 第一阶段：WPF 原生 BitmapImage / WIC
  * 第二阶段：ImageSharp
  * 第三阶段：Magick.NET，可选支持更多专业格式
* 配置保存：JSON 本地配置文件
* 打包方式：

  * 开发阶段：普通 Debug / Release
  * 发布阶段：Self-contained EXE 或 Inno Setup 安装包

---

## 三、产品定位

这是一个“快速预览序列帧动画”的轻量工具，不是完整的视频编辑软件。

优先保证：

* 打开速度快
* 播放稳定
* 操作简单
* UI 清晰
* 内存不会暴涨
* 大量序列帧时不卡死

---

## 四、核心功能范围

### MVP 必须实现

1. 打开文件夹
2. 拖拽文件夹到窗口
3. 自动扫描序列帧
4. 按文件名自然排序
5. 显示第一帧
6. 播放 / 暂停
7. 上一帧 / 下一帧
8. FPS 设置
9. 循环播放开关
10. 显示当前帧 / 总帧数
11. 显示图片分辨率
12. 窗口缩放时自动适配预览图
13. 支持透明 PNG 棋盘格背景
14. 最近打开文件夹记录

### 第二阶段功能

1. 时间轴滑块拖动
2. 键盘快捷键
3. 缩放倍率显示
4. 鼠标滚轮缩放
5. 鼠标拖动画布
6. 缓存前后帧
7. 播放时预读后续帧
8. 支持文件夹内容变化自动刷新
9. 支持打开单张图片后自动识别同目录序列
10. 支持深色 / 浅色主题

### 第三阶段功能

1. 支持导出 GIF
2. 支持导出 MP4
3. 支持导出指定帧为 PNG
4. 支持设置起始帧 / 结束帧
5. 支持反向播放
6. 支持 ping-pong 往返播放
7. 支持透明背景 / 黑底 / 白底 / 棋盘格切换
8. 支持多种图片格式扩展
9. 支持性能模式设置
10. 支持大图自动降采样预览

---

## 五、软件界面规划

### 主界面布局

采用简洁三段式布局：

```text
┌────────────────────────────────────┐
│ 顶部工具栏                          │
│ 打开文件夹 / FPS / 循环 / 背景模式  │
├────────────────────────────────────┤
│                                    │
│            图片预览区域             │
│                                    │
├────────────────────────────────────┤
│ 播放控制栏 + 时间轴                 │
│ 上一帧 / 播放暂停 / 下一帧 / 帧信息 │
└────────────────────────────────────┘
```

### 顶部工具栏

包含：

* 打开文件夹按钮
* 最近打开按钮
* FPS 输入框
* 循环播放开关
* 背景模式切换：

  * 棋盘格
  * 黑色
  * 白色
* 适配窗口按钮
* 原始大小按钮

### 中间预览区

要求：

* 默认居中显示图片
* 图片等比缩放
* 支持透明 PNG 预览
* 背景支持棋盘格
* 支持鼠标滚轮缩放
* 支持鼠标拖动查看大图
* 空状态时显示“拖入序列帧文件夹开始预览”

### 底部播放控制区

包含：

* 上一帧按钮
* 播放 / 暂停按钮
* 下一帧按钮
* 时间轴滑块
* 当前帧编号
* 总帧数
* 当前 FPS
* 图片尺寸
* 文件名

---

## 六、项目目录结构

建议使用如下结构：

```text
SequenceFrameViewer
├─ SequenceFrameViewer.sln
├─ src
│  └─ SequenceFrameViewer
│     ├─ App.xaml
│     ├─ MainWindow.xaml
│     ├─ MainWindow.xaml.cs
│     │
│     ├─ Views
│     │  ├─ MainView.xaml
│     │  └─ Controls
│     │     ├─ FramePreviewControl.xaml
│     │     ├─ PlaybackControl.xaml
│     │     └─ TimelineControl.xaml
│     │
│     ├─ ViewModels
│     │  ├─ MainViewModel.cs
│     │  ├─ PlaybackViewModel.cs
│     │  └─ SettingsViewModel.cs
│     │
│     ├─ Models
│     │  ├─ FrameItem.cs
│     │  ├─ FrameSequence.cs
│     │  └─ AppSettings.cs
│     │
│     ├─ Services
│     │  ├─ SequenceScanner.cs
│     │  ├─ FrameDecoder.cs
│     │  ├─ FrameCache.cs
│     │  ├─ PlaybackEngine.cs
│     │  ├─ SettingsService.cs
│     │  └─ FileWatcherService.cs
│     │
│     ├─ Helpers
│     │  ├─ NaturalSortComparer.cs
│     │  ├─ ImageFormatHelper.cs
│     │  └─ RelayErrorHandler.cs
│     │
│     └─ Resources
│        ├─ Themes
│        └─ Icons
│
└─ tests
   └─ SequenceFrameViewer.Tests
```

---

## 七、核心模块设计

## 1. SequenceScanner

负责扫描文件夹中的序列帧。

### 功能要求

* 输入文件夹路径
* 获取支持格式的图片文件
* 按自然顺序排序
* 自动识别文件名中的数字序列
* 返回 FrameSequence 对象

### 支持格式

第一版支持：

```text
.png
.jpg
.jpeg
.webp
.bmp
```


### 排序要求

必须使用自然排序。

例如：

```text
frame_1.png
frame_2.png
frame_10.png
```

不能错误排成：

```text
frame_1.png
frame_10.png
frame_2.png
```

---

## 2. FrameDecoder

负责图片解码。

### 第一阶段方案

使用 WPF 原生 BitmapImage。

要求：

* 使用异步加载
* 加载完成后 Freeze，避免跨线程问题
* 不在 UI 线程中做大量解码
* 捕获损坏图片异常


---

## 3. FrameCache

负责帧缓存。

### 设计原则

不要一次性加载全部序列帧。

第一版缓存策略：

* 当前帧必须缓存
* 当前帧前 5 帧缓存
* 当前帧后 20 帧缓存
* 播放时优先预读后续帧

第二版缓存策略：

* 支持设置最大内存占用
* 默认最大缓存 512MB
* 可选 1GB / 2GB
* 超出后使用 LRU 淘汰策略

### 缓存目标

* 小序列帧：播放流畅
* 大序列帧：不爆内存
* 快速拖动时间轴：尽量减少等待

---

## 4. PlaybackEngine

负责播放控制。

### 功能要求

* 播放
* 暂停
* 停止
* 跳转到指定帧
* 下一帧
* 上一帧
* 循环播放
* 设置 FPS
* 播放完成后根据循环设置决定是否回到第一帧

### 播放计时

第一版可使用 DispatcherTimer。

如果播放不稳定，第二版改为：

* Stopwatch + CompositionTarget.Rendering
* 或独立播放循环 + UI 调度更新

### FPS 范围

建议：

```text
最小 FPS：1
最大 FPS：120
默认 FPS：24
```

---

## 5. RenderService / FramePreviewControl

负责显示图片。

### 功能要求

* 显示当前帧
* 保持比例
* 居中显示
* 支持适配窗口
* 支持原始大小
* 支持缩放
* 支持拖动画布
* 支持棋盘格背景

### 透明背景

PNG 有 Alpha 通道时，默认显示棋盘格。

背景模式：

```text
Checkerboard
Black
White
Gray
TransparentPreview
```

---

## 6. SettingsService

负责本地设置保存。

### 保存内容

```json
{
  "defaultFps": 24,
  "loopPlayback": true,
  "backgroundMode": "Checkerboard",
  "theme": "Dark",
  "recentFolders": [],
  "maxCacheMemoryMb": 512,
  "lastWindowWidth": 1200,
  "lastWindowHeight": 800
}
```

### 保存路径

使用用户 AppData 路径：

```text
%AppData%/SequenceFrameViewer/settings.json
```

---

## 八、数据模型设计

### FrameItem

```csharp
public class FrameItem
{
    public int Index { get; set; }
    public string FilePath { get; set; } = string.Empty;
    public string FileName { get; set; } = string.Empty;
    public long FileSize { get; set; }
    public int? Width { get; set; }
    public int? Height { get; set; }
}
```

### FrameSequence

```csharp
public class FrameSequence
{
    public string FolderPath { get; set; } = string.Empty;
    public List<FrameItem> Frames { get; set; } = new();
    public int TotalFrames => Frames.Count;
    public bool IsEmpty => Frames.Count == 0;
}
```

### AppSettings

```csharp
public class AppSettings
{
    public int DefaultFps { get; set; } = 24;
    public bool LoopPlayback { get; set; } = true;
    public string BackgroundMode { get; set; } = "Checkerboard";
    public string Theme { get; set; } = "Dark";
    public List<string> RecentFolders { get; set; } = new();
    public int MaxCacheMemoryMb { get; set; } = 512;
    public double LastWindowWidth { get; set; } = 1200;
    public double LastWindowHeight { get; set; } = 800;
}
```

---

## 九、开发阶段规划

# 阶段 0：项目初始化

## 目标

搭建 WPF 项目基础结构。

## 任务

1. 创建 .NET WPF 项目。
2. 引入 CommunityToolkit.Mvvm。
3. 建立 MVVM 基础目录。
4. 创建 MainWindow。
5. 创建 MainViewModel。
6. 实现基础窗口布局。
7. 设置应用图标和应用名称。
8. 初始化 SettingsService。

## 验收标准

* 项目可以正常启动。
* 主窗口可以显示。
* UI 分为顶部工具栏、中间预览区、底部播放区。
* 程序关闭后不会报错。
* 设置文件可以正常创建和读取。

---

# 阶段 1：序列帧扫描与加载

## 目标

实现打开文件夹并识别序列帧。

## 任务

1. 实现打开文件夹按钮。
2. 实现拖拽文件夹到窗口。
3. 编写 SequenceScanner。
4. 扫描支持格式的图片。
5. 实现自然排序。
6. 生成 FrameSequence。
7. 在 UI 上显示总帧数。
8. 默认加载第一帧。
9. 显示当前文件名、分辨率、格式。

## 验收标准

* 可以选择一个图片序列文件夹。
* 可以拖入文件夹。
* 图片顺序正确。
* 第一帧可以正常显示。
* 总帧数显示正确。
* 文件名和当前帧编号显示正确。
* 空文件夹要给出友好提示。
* 不支持的文件夹不能导致程序崩溃。

---

# 阶段 2：基础播放功能

## 目标

实现序列帧播放能力。

## 任务

1. 编写 PlaybackEngine。
2. 实现播放按钮。
3. 实现暂停按钮。
4. 实现上一帧。
5. 实现下一帧。
6. 实现 FPS 输入。
7. 实现循环播放。
8. 播放时更新当前帧编号。
9. 播放到末尾时根据循环设置处理。
10. 播放过程中避免 UI 卡死。

## 验收标准

* 点击播放后可以按 FPS 播放序列帧。
* 点击暂停后停在当前帧。
* 上一帧 / 下一帧可用。
* FPS 修改后播放速度变化正确。
* 循环开启时播放到末尾会回到第一帧。
* 循环关闭时播放到末尾自动停止。
* 播放时 UI 仍然可以响应。

---

# 阶段 3：预览区交互优化

## 目标

让图片查看体验接近常用素材查看器。

## 任务

1. 图片默认适配窗口。
2. 添加“适配窗口”按钮。
3. 添加“原始大小”按钮。
4. 实现鼠标滚轮缩放。
5. 实现鼠标拖动画布。
6. 显示当前缩放倍率。
7. 实现棋盘格背景。
8. 支持黑底 / 白底 / 灰底切换。
9. 记住用户背景模式设置。

## 验收标准

* 大图可以自动适配窗口。
* 可以切换原始大小。
* 鼠标滚轮缩放流畅。
* 鼠标可以拖动查看局部。
* 透明 PNG 能明显看出透明区域。
* 背景切换正常。
* 重启软件后保留上次背景设置。

---

# 阶段 4：缓存与性能优化

## 目标

提升播放流畅度，避免大量序列帧导致内存暴涨。

## 任务

1. 编写 FrameCache。
2. 当前帧优先加载。
3. 当前帧后方帧预读。
4. 当前帧前方少量缓存。
5. 播放时异步预加载后续帧。
6. 限制最大缓存数量。
7. 统计大致缓存内存。
8. 超出限制时释放远离当前帧的缓存。
9. 加载损坏图片时跳过并提示。
10. 对超大图增加预览降采样策略。

## 验收标准

* 500 张 1080p PNG 序列帧可以正常播放。
* 播放过程中内存不会无限上涨。
* 快速切换帧不会明显卡死。
* 损坏图片不会导致程序崩溃。
* 关闭文件夹后缓存可以释放。

---

# 阶段 5：时间轴与快捷键

## 目标

提升操作效率。

## 任务

1. 添加时间轴 Slider。
2. 拖动 Slider 跳转帧。
3. 播放时 Slider 同步更新。
4. 添加键盘快捷键：

   * Space：播放 / 暂停
   * Left：上一帧
   * Right：下一帧
   * Home：第一帧
   * End：最后一帧
   * Ctrl + O：打开文件夹
   * Ctrl + R：重新加载
   * F：适配窗口
   * 1：原始大小
5. 快捷键不能影响输入框正常输入。

## 验收标准

* 时间轴可以准确跳转。
* 播放时进度条同步。
* 快捷键响应正确。
* FPS 输入框聚焦时，数字输入不被快捷键干扰。

---

# 阶段 6：文件监听与最近记录

## 目标

适合 AIGC / 渲染输出场景，可以边生成边查看。

## 任务

1. 使用 FileSystemWatcher 监听当前文件夹。
2. 文件新增时自动刷新序列。
3. 文件删除时自动更新。
4. 添加手动刷新按钮。
5. 添加最近打开文件夹列表。
6. 最近记录最多保存 10 个。
7. 不存在的历史路径自动标记或移除。
8. 支持打开上次使用的文件夹。

## 验收标准

* 文件夹新增图片后软件能检测到。
* 删除图片后总帧数会更新。
* 最近打开记录可用。
* 重启后最近记录仍然存在。
* 文件变动频繁时不会疯狂刷新导致卡死，需要做防抖处理。

---

# 阶段 7：打包与发布

## 目标

生成可交付的 Windows 工具。

## 任务

1. 配置 Release 构建。
2. 生成单文件 EXE。
3. 测试 self-contained 发布。
4. 添加应用图标。
5. 添加版本号。
6. 添加关于页面。
7. 使用 Inno Setup 制作安装包。
8. 支持便携版 ZIP。
9. 编写 README。
10. 编写用户使用说明。

## 验收标准

* 可以在干净 Windows 环境运行。
* 不需要用户手动安装复杂依赖。
* EXE 可以正常启动。
* 安装包可以正常安装和卸载。
* 便携版解压即可使用。
* README 能说明基本用法。

---

## 十、性能要求

### 启动性能

* 冷启动尽量控制在 2 秒内。
* 不要在启动时加载大量资源。
* 最近记录和设置读取应非常轻量。

### 打开文件夹性能

* 100 张图片：应几乎即时完成。
* 500 张图片：应在可接受时间内完成。
* 1000 张图片：允许稍等，但 UI 不能卡死。
* 扫描过程需要异步执行。

### 播放性能

目标：

* 1080p PNG 序列帧 24 FPS 基本流畅。
* 2K 图片可以正常预览。
* 4K 图片允许降低预览质量以保证响应速度。
* 不追求无损实时 4K 60 FPS，除非后续进入专业性能优化阶段。

### 内存要求

* 默认缓存上限 512MB。
* 不允许因为打开大量图片导致内存无限增长。
* 关闭当前序列后必须释放缓存。

---

## 十一、异常处理要求

必须处理以下异常：

1. 文件夹不存在。
2. 文件夹为空。
3. 文件夹中没有支持格式。
4. 图片文件损坏。
5. 图片正在被其他程序写入。
6. 文件被删除。
7. 权限不足。
8. 路径过长。
9. 解码失败。
10. 内存不足。

所有异常都不能导致程序直接崩溃。

提示方式：

* 非严重问题显示在状态栏。
* 严重问题弹出简洁提示。
* 详细错误写入日志。

---

## 十二、日志系统

第一版可以使用简单文本日志。

日志路径：

```text
%AppData%/SequenceFrameViewer/logs
```

记录内容：

* 软件启动
* 打开文件夹
* 扫描结果
* 图片加载失败
* 播放异常
* 缓存释放
* 未捕获异常

后续可接入 Serilog。

---

## 十三、UI 风格要求

整体风格：

* 简洁
* 深色优先
* 类似素材查看器
* 不要复杂花哨
* 不要广告感
* 不要过度拟物

推荐视觉方向：

```text
深灰背景
浅灰文字
蓝色或青色作为主强调色
按钮圆角轻微
预览区保持干净
底部控制栏紧凑
```

---

## 十四、代码规范

1. 使用 MVVM，不要把大量逻辑写进 MainWindow.xaml.cs。
2. ViewModel 不直接操作文件系统，文件操作放到 Service。
3. 所有耗时任务必须 async。
4. UI 更新必须回到 UI 线程。
5. 图片解码完成后尽量 Freeze。
6. 所有 public 方法要有清晰命名。
7. 不要写死路径。
8. 不要写死 UI 文案到业务逻辑中。
9. 配置项集中管理。
10. 关键模块需要单元测试。

---

## 十五、单元测试建议

优先测试以下模块：

1. NaturalSortComparer
2. SequenceScanner
3. SettingsService
4. FrameCache
5. PlaybackEngine

测试用例示例：

### 自然排序

输入：

```text
frame_1.png
frame_10.png
frame_2.png
```

期望输出：

```text
frame_1.png
frame_2.png
frame_10.png
```

### 空文件夹

输入空文件夹路径。

期望：

* 不崩溃
* 返回空 FrameSequence
* UI 提示无可用图片

### FPS 设置

输入：

```text
0
-1
24
60
999
```

期望：

* 小于 1 时修正为 1
* 大于 120 时修正为 120
* 正常值保持

---

## 十六、建议开发顺序

严格按照以下顺序开发：

1. 项目初始化
2. 主界面布局
3. 设置服务
4. 打开文件夹
5. 序列帧扫描
6. 第一帧显示
7. 上一帧 / 下一帧
8. 播放 / 暂停
9. FPS 控制
10. 循环播放
11. 时间轴
12. 图片缩放与拖动
13. 棋盘格背景
14. 缓存系统
15. 文件夹监听
16. 最近打开记录
17. 异常处理
18. 日志
19. 打包
20. README

不要一开始就做导出 GIF / MP4。先把查看和播放体验做好。

---

## 十七、第一版 MVP 验收清单

第一版完成后，必须满足：

* [ ] 可以正常启动软件
* [ ] 可以打开图片序列文件夹
* [ ] 可以拖拽文件夹打开
* [ ] 可以正确排序序列帧
* [ ] 可以显示第一帧
* [ ] 可以播放
* [ ] 可以暂停
* [ ] 可以上一帧 / 下一帧
* [ ] 可以设置 FPS
* [ ] 可以循环播放
* [ ] 可以显示当前帧编号
* [ ] 可以显示总帧数
* [ ] 可以显示当前文件名
* [ ] 可以显示图片尺寸
* [ ] 可以适配窗口显示
* [ ] 透明 PNG 有棋盘格背景
* [ ] 打开空文件夹不会崩溃
* [ ] 图片损坏不会崩溃
* [ ] 关闭软件不会报错

---

## 十八、后续扩展方向

第一版稳定后，再考虑：

1. 导出 GIF
2. 导出 MP4
3. 支持音频预览
4. 支持对比两组序列帧
5. 支持 A/B 播放
6. 支持洋葱皮预览
7. 支持帧差异高亮
8. 支持 PSD / EXR / TGA
9. 支持命令行打开文件夹
10. 支持作为素材管理器的预览组件
11. 支持与 ComfyUI / SD / AIGC 输出目录联动
12. 支持监控渲染输出进度

---

## 十九、开发注意事项

1. 不要一次性加载全部帧。
2. 不要让图片解码阻塞 UI 线程。
3. 不要为了 UI 好看牺牲播放稳定性。
4. 不要一开始做复杂导出功能。
5. 不要使用 Electron。
6. 第一版重点是“打开快、播放稳、不卡死”。
7. 图片显示逻辑、播放逻辑、缓存逻辑必须拆开。
8. 所有文件读取都要考虑异常。
9. 播放器状态要清晰，例如 Idle / Loading / Playing / Paused / Error。
10. 代码结构要方便后续替换渲染方案，例如未来从 WPF Image 控件升级到更高性能的渲染方式。

---

## 二十、最终交付物

开发完成后需要交付：

1. WPF 项目源码
2. 可运行 EXE
3. 便携版 ZIP
4. 安装包
5. README.md
6. 使用说明
7. 简单测试用例
8. 版本更新记录 CHANGELOG.md

---

## 二十一、优先级总结

### P0：必须完成

* 打开文件夹
* 扫描序列帧
* 自然排序
* 显示图片
* 播放 / 暂停
* FPS 设置
* 上一帧 / 下一帧
* 循环播放
* 基础异常处理

### P1：强烈建议完成

* 时间轴
* 缩放
* 拖动画布
* 棋盘格背景
* 最近打开
* 缓存优化
* 文件夹监听

### P2：后续增强

* 导出 GIF
* 导出 MP4
* 更多格式支持
* 对比预览
* 洋葱皮
* 专业渲染优化

---

## 二十二、给 AI 开发 Agent 的执行要求

请按照以上规划分阶段开发。每完成一个阶段后，先进行自测，再进入下一阶段。

开发时请遵守以下原则：

1. 先实现可运行 MVP。
2. 每个阶段都要保证项目可以编译通过。
3. 不要一次性生成大量未经验证的复杂代码。
4. 每个核心模块都要保持职责单一。
5. 发现需求冲突时，优先保证轻量、稳定、可维护。
6. 对于性能相关功能，先做简单可用版本，再做优化版本。
7. 所有异常都要优雅处理，不能让软件直接崩溃。
8. 最终项目需要能在 Windows 上直接运行。

最终目标是做出一个轻量、稳定、好用的 Windows 序列帧查看器。
