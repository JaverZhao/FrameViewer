<Window x:Class="SequenceFrameViewer.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:resx="clr-namespace:SequenceFrameViewer.Resources"
        Title="{Binding [AboutTitle], Source={x:Static resx:LocalizationService.Default}}"
        Height="280" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource PanelBackgroundBrush}"
        Foreground="{StaticResource ForegroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="FrameView" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" />
        <TextBlock x:Name="VersionText" Grid.Row="1" FontSize="14" Margin="0,4,0,0" />
        <TextBlock Grid.Row="2" Text="{Binding [AppDescription], Source={x:Static resx:LocalizationService.Default}}" FontSize="13" Margin="0,8,0,0" Foreground="#AAAAAA" />

        <Separator Grid.Row="3" Margin="0,12,0,8" Background="#3E3E3E" />

        <TextBlock Grid.Row="4" Text="{Binding [AboutDescription], Source={x:Static resx:LocalizationService.Default}}"
                   TextWrapping="Wrap" FontSize="12" Foreground="#888888"
                   VerticalAlignment="Top" />

        <Button Grid.Row="5" Content="{Binding [Ok], Source={x:Static resx:LocalizationService.Default}}" Width="80" Height="32" HorizontalAlignment="Right"
                Click="OnOkClick"
                Background="{StaticResource AccentBrush}"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6" />
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
